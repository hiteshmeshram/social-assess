import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // You can add allowed image domains here if needed, otherwise leave it empty as you have.
    domains: [],
  },
 
};

export default nextConfig;